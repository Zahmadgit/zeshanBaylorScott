import {View, Text} from 'react-native';
// Normally I wouldnt use an emoji for the icon, but this keeps the bundle size smaller
const FlaggedTabIcon = () => (
  <View>
    <Text>🚩</Text>
  </View>
);

export default FlaggedTabIcon;
